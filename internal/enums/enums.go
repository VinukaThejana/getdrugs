package enums

type env string

const (
	// Dev is the development environment
	Dev env = "dev"
	// Stg is the staging environment
	Stg env = "stg"
	// Prd is the production environment
	Prd env = "prd"
)
